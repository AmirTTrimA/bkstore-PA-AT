import enTranslations from './en.json';
import faTranslations from './fa.json';

export const translations = {
  en: enTranslations,
  fa: faTranslations,
};

export const SUPPORTED_LANGUAGES = ['en', 'fa'];
export const DEFAULT_LANGUAGE = 'en';

export default translations;
